defaults: &defaults
  app: default
  probabilities:
    default: 0.502
    max: 0.95
    min: 0.5
    deposits:
      initial: 1
  rewards:
    friendship:
      default: 10
    signup:
      default: 20
    post:
      default: 20
    comment:
      default: 15
    like:
      default: 10
    dm:
      default: 10
  percentages:
    default: 10

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
